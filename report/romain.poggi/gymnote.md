# Run log — romain.poggi @ gymnote

Machine-local experiment log. Other hosts/users have their own files under `report/{user}/{host}.md`. Do not edit this file from another account or machine.

---

## Training data and validation protocol (Track A stab runs)

These runs use the standard **official-val holdout** setup in `train.py` when `dataset.official_val_holdout_ratio=0.1` (see [`diver_CL_head_continue`](../../experiments/diver_CL_head_continue.md)).

| Split | Clips | In training? | Used for |
|-------|------:|--------------|----------|
| `data/train` | 44,993 | yes | train |
| **90% of `data/val`** (stratified, `dataset.seed`) | 6,069 | **yes** | train + logged as **honest val** |
| **10% holdout** (same stratified split) | 676 | **no** | **best checkpoint, early stopping** |
| **Honest val** (= all 6,745 official val clips) | 6,745 | 90% overlap with train | monitoring only |

**Checkpoint selection is not leaky:** `Saved new best checkpoint` uses **holdout top-1 only** (`holdout_stats` / `ema_holdout_stats` in `src/smth2smth/pipelines/train.py`). Honest val is logged each epoch but does not drive `best_top1`.

**Why honest val looks ~7–10 pp higher than holdout:** ~90% of honest-val clips were also in the training set (memorization / familiarity). Example at ep21 on this run: holdout **57.25%**, clips-from-val-in-train **64.13%**, honest **63.44%** (= weighted mix). Do **not** use honest val for model selection or LB expectations.

**Holdout split:** `split_train_val_stratified(..., val_ratio=0.1, seed=int(cfg.dataset.seed))` — for `arch2-perceiver-q16-stab-s42`, **`seed=123`** (not 42). Holdout **N = 676**.

Log line (typical):

```text
[data] train=51062 (train_dir=44993 + val_dir=6069 (90% stratified)), val_holdout=676 / val_total=6745
[data] dual val eval each epoch: holdout n=676 (checkpoint/early-stop), honest n=6745 (full official val; includes clips also used in training)
```

---

## arch2-perceiver-q16-stab-s42 | Track A | gymnote

Seed replica of stabilized Perceiver Q=16 (`track_a_diverse_arch2_perceiver_stab_s42`, `seed=123`). SSL init: `videomaev2_t4native_encoder_ep500.pt`.

| Item | Value |
|------|--------|
| **Status** | RUNNING (resumed 2026-05-26 after mid-ep28 stop) |
| **Experiment** | [`diver_CL_head_continue`](../../experiments/diver_CL_head_continue.md) |
| **Hydra** | `experiment=track_a_diverse_arch2_perceiver_stab_s42` `seed=123` |
| **Log** | [`arch2-perceiver-q16-stab-s42_20260525.log`](../../logs/track_a/arch2-perceiver-q16-stab-s42_20260525.log) |
| **W&B** | [f4mlyixh](https://wandb.ai/romain-poggi-ecole-polytechnique/smth2smth-diverse-heads/runs/f4mlyixh) |
| **PID** | [`arch2-perceiver-q16-stab-s42_20260525.pid`](../../logs/track_a/arch2-perceiver-q16-stab-s42_20260525.pid) |
| **Best ckpt** | `checkpoints/track_a/videomaev2+ft/arch2-perceiver-q16-stab-s42.pt` |

### Training metrics (holdout top-1 — selection metric)

| Epoch | Holdout top-1 | Honest top-1 (inflated) | Notes |
|------:|----------------:|------------------------:|-------|
| 21 | 0.5725 | 0.6344 | used for ep21 Kaggle submits below |
| 27 | 0.5917 | 0.6824 | best before first stop |
| 28 | 0.5947 | 0.6789 | after resume |
| 33 | 0.6213 | 0.7317 | |
| 35 | 0.6243 | 0.7376 | latest at doc time |

Early stopping patience **15** on **holdout** only.

---

## Submissions — ep21 checkpoint (`arch2-perceiver-q16-stab-s42-ep21-best.pt`)

Frozen snapshot of live best at ep21 (holdout **57.25%**). Champion TTA recipe: [`results_tta_ensembling`](../../experiments/results_tta_ensembling.md) — scales `[0.857, 1.0, 1.143]` + horizontal flip (6 views).

### Holdout ablation (same 676 clips, `seed=123`, ep21 ckpt)

| Setting | Holdout top-1 | vs no TTA |
|---------|---------------|-----------|
| **No TTA** | **57.25%** | — |
| Champion TTA (submit path, softmax avg) | 57.10% | −0.15 pp |
| Champion TTA (logit avg) | 57.25% | 0.00 pp |
| 3-scale, no flip (ckpt `tta_flip=false`) | 57.69% | +0.44 pp |

**Conclusion:** TTA does not improve holdout for this model; Kaggle gap vs holdout is mostly generalization / test distribution, not TTA gain on val.

### Kaggle public LB

| CSV | TTA | LB top-1 | Notes |
|-----|-----|----------|-------|
| `track_a_arch2-perceiver-q16-stab-s42_ep21_champion_tta_20260525.csv` | champion | **3.96%** | **broken submit** (see below) |
| [`track_a_arch2-perceiver-q16-stab-s42_ep21_champion_tta_20260525_v2.csv`](../../submissions/track_a_arch2-perceiver-q16-stab-s42_ep21_champion_tta_20260525_v2.csv) | champion (fixed) | **51.95%** | correct `pool_head` path |
| [`track_a_arch2-perceiver-q16-stab-s42_ep21_notta_20260525.csv`](../../submissions/track_a_arch2-perceiver-q16-stab-s42_ep21_notta_20260525.csv) | none | *(pending upload)* | same ckpt, `training.tta=false` |

Submit logs: [`…-v2_champion-tta_20260525.log`](../../logs/track_a/arch2-perceiver-q16-stab-s42-submit-ep21-v2_champion-tta_20260525.log), [`…-notta_20260525.log`](../../logs/track_a/arch2-perceiver-q16-stab-s42-submit-ep21-notta_20260525.log).

---

## Bug: Perceiver head skipped in ViT submit TTA path (fixed 2026-05-25)

**Symptom:** First champion submit **~4% LB** despite **~57% holdout** at ep21; predictions collapsed (e.g. class 31 ≈23% of rows).

**Cause:** `smth2smth.pipelines.submit._videomae_logits_batch` (and `ensemble/inference.py` copy) pooled encoder tokens with **`mean(dim=1)`** when `attn_pool` was absent, instead of **`pool_head`** (Perceiver). Training and holdout eval use `VideoMAEViT.forward()` → `pool_head`; submit did not.

**Fix:** `elif getattr(model, "pool_head", None) is not None: pooled = model.pool_head(features)` in `submit.py` and `ensemble/inference.py`. Regression test: `tests/pipelines/test_submit_tta.py::TestVideomaePerceiverSubmitPath`.

**Do not upload** CSVs generated before this fix (no `_v2` suffix).

---

## Event log (chronological)

### 2026-05-25 — train start

- Launched `arch2-perceiver-q16-stab-s42` on gymnote; log [`arch2-perceiver-q16-stab-s42_20260525.log`](../../logs/track_a/arch2-perceiver-q16-stab-s42_20260525.log).

### 2026-05-25 — submits (ep21 ckpt)

- Champion submit v1 → LB **3.96%** (bug).
- Champion submit v2 (fixed) → LB **51.95%**.
- No-TTA submit generated; LB pending.

### 2026-05-26 — train resume

- Stopped mid-ep28; resumed from `arch2-perceiver-q16-stab-s42.last.pt`, W&B `f4mlyixh`, appended to same log.
